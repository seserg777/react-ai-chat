# Server only — copy to server/.env or repo root if you load dotenv from root
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Optional: model context limit for UI "remaining" estimate (input vs window); e.g. 1000000 for many Gemini models.
GEMINI_CONTEXT_WINDOW=
PORT=3001
